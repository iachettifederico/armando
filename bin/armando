#!/usr/bin/env ruby

require "armando"

require "awesome_print"
AwesomePrint.defaults = {
  indent: 2,
  index:  false,
}

generator = ARGV[0]
arguments = ARGV[1..-1]

Armando.for(generator, arguments).generate!
